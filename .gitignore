# =====================================================================
# HEAVY dataset files stay OUT of git  regenerate via scripts/get_data.sh
# or ./train.sh full (see docs/data_setup.md). Model bundles (small, a few
# hundred KB) ARE committed so the repo is usable right after clone.
# =====================================================================
# Raw PaySim dataset (~471MB)  download from Kaggle into data/raw/
data/raw/*
!data/raw/.gitkeep

# Kaggle download archive left in the repo root (~178MB)
*.zip

# Big processed datasets  regenerate: ./scripts/get_data.sh
# NOTE: transactions_context.parquet used to be whitelisted here (~9MB on the
# 100k stand-in), but on the REAL full PaySim data it is ~526MB and exceeds
# GitHub's 100MB hard limit. It is now gitignored like the others.
data/processed/*.parquet
# ...EXCEPT the small (~28MB) stratified sample the app falls back to when the
# full frame is absent, so a fresh clone runs every page (in "sample mode").
# Regenerate: python src/make_context_sample.py
!data/processed/context_sample.parquet
# ...and the precomputed scored test split the Overview/Cost pages read directly
# (~1s load instead of ~10min live scoring). Regenerate: python src/make_scored_test.py
!data/processed/scored_test.parquet

# Synthetic caches (regenerate on first run)  customer master + stand-in
data/synthetic/*
!data/synthetic/.gitkeep

# Large generated HTML report (~4MB)  regenerate: python monitoring/drift.py
monitoring/reports/*.html

# Runtime logs
logs/*.log
train.log
train_realistic.log

# Unrelated one-off script (different project's dataset download)
install_kaggle.py

# =====================================================================
# KEPT in git on purpose (small + useful after clone):
#   models/*.joblib                     (trained bundle  run infer.py/api right after clone)
#   data/processed/sample_preview.csv   (Streamlit demo runs without big files)
#   docs/figures/*.png, docs/*.md, monitoring/reports/*.md
# =====================================================================

# Python
__pycache__/
*.pyc
.venv/
venv/
.ipynb_checkpoints/

# OS / editor
.DS_Store
desktop.ini
Thumbs.db
.idea/
.vscode/
scored.csv

# Secrets  Streamlit secrets (webhook URLs, tokens). NEVER commit.
.streamlit/secrets.toml
.env

# LaTeX build artifacts (report/main.tex + figures + main.pdf ARE kept)
report/*.aux
report/*.log
report/*.out
report/*.fls
report/*.fdb_latexmk
report/*.toc
report/*.synctex.gz
report/build*.log

# Beamer/LaTeX build artifacts for the presentation (.tex + .pdf + images ARE kept)
report/Group 1 Presentation Slides/*.aux
report/Group 1 Presentation Slides/*.log
report/Group 1 Presentation Slides/*.out
report/Group 1 Presentation Slides/*.fls
report/Group 1 Presentation Slides/*.fdb_latexmk
report/Group 1 Presentation Slides/*.toc
report/Group 1 Presentation Slides/*.nav
report/Group 1 Presentation Slides/*.snm
report/Group 1 Presentation Slides/*.synctex.gz
report/Group 1 Presentation Slides/build*.log
